{
  "name": "puzzlewithme",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "vitest run",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "trustedDependencies": ["better-sqlite3", "esbuild", "sharp"],
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
